export function excellentRepoSkillInstruction(excellentRepoSkillPath: string): string {
  return `Load and follow the skill at ${excellentRepoSkillPath}. It describes what an excellent repository should be, and some code style rules.`;
}
